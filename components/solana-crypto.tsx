export function SolanaCrypto({ className = "" }: { className?: string }) {
  return (
    <span
      className={`bg-gradient-to-br from-[#9945FF] to-[#14F195] bg-clip-text text-transparent ${className}`}
    >
      Crypto
    </span>
  );
}
